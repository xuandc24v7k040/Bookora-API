enum UserType {
  SYSTEM
  BRANCH
  CUSTOMER
}

enum PermissionEffect {
  ALLOW
  DENY
}

enum AuthProvider {
  LOCAL
  GOOGLE
}

enum AuthAttemptType {
  EMAIL
  IP
  PASSWORD_RESET_EMAIL
  PASSWORD_RESET_IP
}
