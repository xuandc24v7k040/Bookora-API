// Service-level rules intentionally not enforced directly by Prisma:
// 1. ACTIVE Products must have at least one active ProductVariant and exactly one active default.
// 2. Single Products use one default ProductVariant with combinationKey DEFAULT.
// 3. Variant combination keys are generated deterministically from sorted option/value pairs.
// 4. ProductMedia.variantId, when present, must reference a Variant owned by productId.
// 5. StockReceipt DRAFT does not change stock; CONFIRMED increments stock atomically once.
// 6. Checkout and cancellation update BranchProductStock atomically and use the Order stock
//    timestamps to prevent duplicate deduction or restoration.
// 7. Branch-scoped operations must resolve the active selected UserBranch before authorization.
